import { StyledLocationInput } from "./LocationInput.styled";

export const LocationInput = ({ onClick, placeholder }) => {
  return <StyledLocationInput onClick={onClick} placeholder={placeholder} />;
};
